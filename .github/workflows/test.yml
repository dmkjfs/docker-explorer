name: test

on: push

jobs:
  test:
    name: ruff
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-go@v5
          with:
            go-version: '^1.24.0'
        - run: go mod download
        - run: go test ./... -v
